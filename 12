#include <iostream>
using namespace std;
int main()
{
    setlocale(LC_ALL, "Russian");
    int f, s;
    float rez;
    char d;
    cout << "Программа, выполняет действия, обратные тем, которые описаны в упражнении 10, то есть запрашивает у пользователя сумму, указанную в десятичных фунтах, и переводит ее в старую систему фунтов, шиллингов и пенсов.";
    cout << " Введите число десятичных фунтов: ";
    cin >> f >> d >> s;
    rez = (s*2.4)/12; 
    cout <<"Эквивалентная сумма в старой форме записи: £" << f << d << rez << endl;
}
